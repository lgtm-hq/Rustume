{
  "$comment": "Single source of truth for username validation, consumed by crates/server/src/auth/username.rs (include_str!) and apps/web/src/api/account.ts (JSON import). `pattern` encodes the charset and hyphen rules: lowercase alphanumerics with single interior hyphens. Keep `reserved` lowercase and sorted.",
  "min_length": 3,
  "max_length": 32,
  "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
  "reserved": [
    "account",
    "admin",
    "api",
    "auth",
    "billing",
    "dashboard",
    "export",
    "help",
    "import",
    "login",
    "logout",
    "me",
    "r",
    "register",
    "resume",
    "root",
    "rustume",
    "settings",
    "signup",
    "support",
    "www"
  ]
}
