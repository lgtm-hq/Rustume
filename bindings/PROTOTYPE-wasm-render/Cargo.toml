[package]
name = "prototype-wasm-render"
version = "0.0.0"
edition = "2021"
publish = false

# PROTOTYPE — THROWAWAY. Answers one question: does rustume-render compile to
# wasm32-unknown-unknown, and how big is the artifact? Delete after the verdict
# lands on the issue. Do not build on this.

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rustume-render = { path = "../../crates/render" }
rustume-schema = { path = "../../crates/schema" }
wasm-bindgen = "0.2"
serde_json = "1.0"
