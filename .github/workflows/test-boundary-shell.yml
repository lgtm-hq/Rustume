# SPDX-License-Identifier: AGPL-3.0-only
---
name: Test - Boundary Shell Scripts

"on":
  pull_request:
    branches: [main]
    paths:
      - 'scripts/ci/boundary/**'
      - 'tests/bats/**'
      - '.github/workflows/test-boundary-shell.yml'
  push:
    branches: [main]
    paths:
      - 'scripts/ci/boundary/**'
      - 'tests/bats/**'
      - '.github/workflows/test-boundary-shell.yml'

permissions:
  contents: read

concurrency:
  group: test-boundary-shell-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  shell-tests:
    permissions:
      contents: read
      pull-requests: write # publish-test-summary posts shell coverage on PRs
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-test-shell.yml@d32388495d571a0ee13383bacc732765bf4e9e7d # v0.74.6
    with:
      tooling-ref: 'd32388495d571a0ee13383bacc732765bf4e9e7d' # v0.74.6
      job-name: Boundary Shell Tests
      test-path: tests/bats/unit/boundary
      coverage: true
      coverage-threshold: 0
      upload-coverage: false
      egress-policy: block
      egress-preset: github-tooling
      runner-image: ubuntu-24.04
