# SPDX-License-Identifier: AGPL-3.0-only
---
name: Quality - Lint & Format
# Runs lintro quality analysis on PRs and pushes using Docker.

"on":
  push:
    branches: [main]
    paths:
      - "**.rs"
      - "**.md"
      - "**.yml"
      - "**.yaml"
      - "**.toml"
      - "**.sh"
      - "**.ts"
      - "**.tsx"
      - "**.js"
      - "**.mjs"
      - "**.astro"
      - "**.json"
      - "Cargo.toml"
      - "Cargo.lock"
      - "Dockerfile*"
      - ".github/workflows/**"
      - "scripts/**"
      - ".lintro-config.yaml"
      - ".lintro-ignore"
  pull_request:
    branches: [main]

permissions: {}

concurrency:
  group: lint-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    if: >-
      github.event_name != 'pull_request' ||
      github.event.pull_request.draft == false
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-quality-lint.yml@617df664f7ff0b4c2b85126e3c183ad744617dee # v0.44.0
    permissions:
      contents: read
      packages: read
    with:
      tooling-ref: '617df664f7ff0b4c2b85126e3c183ad744617dee' # v0.44.0
      job-name: "🛠️ Lintro Code Quality & Analysis"
      lintro-tool-options: clippy:timeout=300
      timeout-minutes: 30
      runner-image: ubuntu-24.04
      # yamllint disable-line rule:line-length
      lintro-image: ghcr.io/lgtm-hq/py-lintro:0.61.0@sha256:95b9fd9d62c3ca1227447467767ec915e78a7eef62282dd18c0b1c02ee4d3887
      egress-policy: block
      egress-preset: quality

  publish-pr-comment:
    needs: quality
    if: >-
      always() &&
      github.event_name == 'pull_request' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.draft == false
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-publish-quality-summary.yml@617df664f7ff0b4c2b85126e3c183ad744617dee # v0.44.0
    permissions:
      contents: read
      pull-requests: write
    with:
      exit-code: >-
        ${{ needs.quality.outputs.exit-code != '' && needs.quality.outputs.exit-code || '1' }}
      tooling-ref: '617df664f7ff0b4c2b85126e3c183ad744617dee' # v0.44.0
      job-name: "🛠️ Lintro Code Quality & Analysis PR Comment"
      runner-image: ubuntu-24.04
      egress-policy: block
